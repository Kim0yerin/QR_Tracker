<UserControl x:Class="QR_Tracker.View.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QR_Tracker.View"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="100"/>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <DockPanel HorizontalAlignment="Right">
                <TextBlock Text="형태 : "/>
                <ComboBox Margin="0,0,10,0" ItemsSource="{Binding FormatOptions}" SelectedItem="{Binding FormatSelectedItem}">
                </ComboBox>
                <TextBlock Text="기간 : "/>
                <ComboBox ItemsSource="{Binding DayOptions}" SelectedItem="{Binding DaySelectedItem}">
                </ComboBox>
                <Button Content="보기" Command="{Binding ShowReportCommand}" Style="{StaticResource ReportButton}" Width="60" Margin="10,0,10,0" />
            </DockPanel>
            <!-- 제목 -->
            <TextBlock Margin="10" HorizontalAlignment="Center" FontSize="20" Text="{Binding ChartTitle}"/>
            <DataGrid ItemsSource="{Binding DataGrid}" Visibility="{Binding bIsTableFormat, Converter={StaticResource BoolToVisibilityConverter}}">
                
            </DataGrid>
            <lvc:CartesianChart Series="{Binding SeriesData}" LegendLocation="Top" Visibility="{Binding bIsNotTableFormat, Converter={StaticResource BoolToVisibilityConverter}}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="{Binding Loc[ChartXTime]}" Labels="{Binding XLabels}" >
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="{Binding Loc[ChartYEmployeeNum]}" Labels="{Binding YValues}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
            <DockPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="CSV 다운로드" Style="{StaticResource ReportButton}" Command="{Binding ExportCSVCommand}" Visibility="{Binding bIsTableFormat, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>

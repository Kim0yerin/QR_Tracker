<UserControl x:Class="QR_Tracker.View.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QR_Tracker.View"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <DockPanel HorizontalAlignment="Right">
                <Button Content="일일 리포트" Command="{Binding ShowDailyReportCommand}" Style="{StaticResource ReportButton}" />
                <Button Content="주간 리포트" Command="{Binding ShowWeekReportCommand}" Style="{StaticResource ReportButton}"/>
                <Button Content="월간 리포트" Command="{Binding ShowMonthReportCommand}" Style="{StaticResource ReportButton}"/>
            </DockPanel>
            <TextBlock Margin="10" HorizontalAlignment="Center" FontSize="20" Text="{Binding ChartTitle}"/>
            <lvc:CartesianChart Series="{Binding SeriesData}" LegendLocation="Top">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="시간" Labels="{Binding XLabels}" >
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="인원수" Labels="{Binding YValues}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </StackPanel>
    </Grid>
</UserControl>

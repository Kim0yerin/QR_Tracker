<UserControl x:Class="QR_Tracker.View.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QR_Tracker.View"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <DockPanel HorizontalAlignment="Right">
                <Button Content="{Binding Loc[DailyReportButton]}" Command="{Binding ShowDailyReportCommand}" Style="{StaticResource ReportButton}" />
                <Button Content="{Binding Loc[WeekReportButton]}" Command="{Binding ShowWeekReportCommand}" Style="{StaticResource ReportButton}"/>
                <Button Content="{Binding Loc[MonthReportButton]}" Command="{Binding ShowMonthReportCommand}" Style="{StaticResource ReportButton}"/>
            </DockPanel>
            <TextBlock Margin="10" HorizontalAlignment="Center" FontSize="20" Text="{Binding ChartTitle}"/>
            <lvc:CartesianChart Series="{Binding SeriesData}" LegendLocation="Top">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="{Binding Loc[ChartXTime]}" Labels="{Binding XLabels}" >
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="{Binding Loc[ChartYEmployeeNum]}" Labels="{Binding YValues}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </StackPanel>
    </Grid>
</UserControl>

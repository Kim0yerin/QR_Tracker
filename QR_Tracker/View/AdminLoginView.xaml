<UserControl x:Class="QR_Tracker.View.AdminLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QR_Tracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="150"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="17"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <DockPanel Margin="5">
                <Label Content="ID : " />
                <TextBox Text="{Binding strAdminId, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            </DockPanel>
            <DockPanel Margin="5" >
                <Label Content="PassWord : " />
                <PasswordBox x:Name="PasswordBox"/>
            </DockPanel>
            <Button Content="Login" Margin="0,20,0,0" Command="{Binding AdminLoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}"/>
        </StackPanel>

    </Grid>
</UserControl>

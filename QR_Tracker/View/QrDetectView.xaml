<UserControl x:Class="QR_Tracker.View.QrDetectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:viewmodel="clr-namespace:QR_Tracker.ViewModel" 
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <TextBlock Text="{Binding Loc[QrDetectGuide]}" HorizontalAlignment="Center" Margin="0,50,0,0" VerticalAlignment="Top" FontSize="15"></TextBlock>
        <Button Content="↺" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding CameraRefreshCommand}"/>
        <Image Width="540" Height="320" Source="{Binding ImageSource, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding IsTimeValid, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>
